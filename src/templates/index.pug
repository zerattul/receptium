<!DOCTYPE html>
html(lang="en")
head
  meta(charset="UTF-8")
  meta(name="viewport", content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0")
  each css in files.CSS
    link(rel="stylesheet", href=`${css}`)
  title Receptium

body
  section.home
    .home-title
      h1.home-title__title Bienvenido al sistema de captura para administración de invitados, inicia sesión para continuar.
    .home-login
      input(tpye="name" name="name" placeholder="Usuario").mx-login__input
      input(type="password" name="password" placeholder="Contraseña").mx-login__input
      button.mx-login__btn Iniciar
      .home-login-links
        a(href="#").home-login-links__text ¿Olvistaste tu contraseña?
        a(href="#").home-login-links__text.home-login__text--small Información del Servicio

  each js in files.JS
    script(src=`${js}`)
